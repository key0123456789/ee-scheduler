= form_for @choise do |f|
  #error_explanation

.field
  = f.label :college
  = f.text_field :college
.field
  = f.label :fuculty
  = f.text_field :fuculty
.field
  = f.label :department
  = f.text_field :department
.field
  = f.label :condition
  = f.text_field :condition



-# = form_for @wish do |f|
-#   - if @wish.errors.any?
-#     #error_explanation
-#       %h2= "#{pluralize(@wish.errors.count, "error")} prohibited this wish from being saved:"
-#       %ul
-#         - @wish.errors.full_messages.each do |message|
-#           %li= message


  -# .field
  -#   = f.label :name, "日程"
  -#   = f.text_field :name
  -# .field
  -#   = f.label :start_time, "日付"
  -#   = f.datetime_select :start_time, discard_hour: true
  -# .actions
  -#   = f.submit 'Save'

  -# .test
  -#   = f.fields_for :tests do |test|
  -#     = test.label :test_name, "段階"
  -#     = test.text_field :test_name, placeholder: "出願日"
  -#     = test.label :test_day, "日程"
  -#     = test.date_field :test_day